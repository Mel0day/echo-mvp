[build]
builder = "dockerfile"

[deploy]
startCommand = "uv run uvicorn echo.main:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"

[deploy.envVars]
ECHO_DATA_DIR = "/data"
